# ── LLM Configuration ──
# Option A: Azure OpenAI
AZURE_OPENAI_API_KEY=your-azure-openai-key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2024-10-21

# Option B: Anthropic (Claude) — used for code review
ANTHROPIC_API_KEY=your-anthropic-key

# Option C: z.ai (GLM) — OpenAI-compatible
ZAI_API_KEY=your-zai-api-key
ZAI_BASE_URL=https://api.z.ai/api/coding/paas/v4
ZAI_MODEL=glm-5

# ── GitHub ──
GITHUB_TOKEN=ghp_your-personal-access-token
GITHUB_WEBHOOK_SECRET=your-webhook-secret
GITHUB_REPOS=owner/repo1,owner/repo2

# ── Database URLs (override docker-compose defaults if needed) ──
# REDIS_URL=redis://localhost:6379/0
# POSTGRES_URL=postgresql+asyncpg://cto:cto_secret@localhost:5432/digital_cto
# QDRANT_URL=http://localhost:6333

# ── Tailscale (Phase 2: OpenClaw Integration) ──
# Generate at: https://login.tailscale.com/admin/settings/keys
TS_AUTHKEY=tskey-auth-your-key-here

# ── OpenClaw Gateway (JARVIS Communication) ──
# Must match the token configured on Mac mini OpenClaw Gateway
OPENCLAW_GATEWAY_TOKEN=digital_cto_2026

# ── Production Security ──
# Comma-separated list of API keys for client authentication
DIGITAL_CTO_API_KEYS=sk_live_xxxxx,sk_live_yyyyy
REQUIRE_AUTH=true  # Set to true in production to require API key auth
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=60

# ── Application ──
LOG_LEVEL=INFO
ENVIRONMENT=development

# ── Development Mode ──
# Set to "true" to bypass auth in local development (NOT FOR PRODUCTION)
DEVELOPMENT_MODE=false
